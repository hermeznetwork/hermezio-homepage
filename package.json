{
  "name": "hermez-landing",
  "version": "1.0.0",
  "description": "Landing page for the Hermez project",
  "author": "iden3",
  "scripts": {
    "lint-css": "stylelint '**/*.css' --fix",
    "start": "live-server --port=3000 --mount=/:src --watch=src",
    "deploy": "gh-pages -d src -b master"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "live-server": "^1.2.1",
    "stylelint": "^13.6.1",
    "stylelint-config-standard": "^20.0.0"
  }
}
